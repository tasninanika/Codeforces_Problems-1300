#include<iostream>
using namespace std;

int main(){
    string s1, s2;
    cin >> s1 >> s2;

    int x = 0;
    for (int i = 0; i < (int)s.size(); i++) {
        if (s[i] == '+'){
            x++;
        }
        else{
            x--;
        }
    }

    return 0;
}
